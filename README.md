# Tanmer Egg

这是一个 Ruby on Rails 的起步项目，目的是让我们跳过每个项目都需要重复编写的基础代码，直接开始写业务逻辑。

```shell
# 录制命令行
asciinema rec asciinema-records/init-rails-project.cast

gem install rails:8.1.1
rails new . --name tanmer-egg --css tailwind

# 查看录制回放
asciinema play asciinema-records/init-rails-project.cast
```