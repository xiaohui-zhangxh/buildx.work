# Tailwind CSS 配置管理规范

> 本文档说明如何管理基础平台和业务项目的 Tailwind CSS 配置，确保在合并代码时不会产生冲突。

## 📋 问题背景

在 BuildX.work 架构中：
- **基础平台**（`engines/buildx_core`）：提供基础设施功能
- **业务项目**（如 `cardx`）：基于基础平台开发的具体业务

当基础平台和业务项目都需要配置 Tailwind CSS 时，需要确保：
1. 配置不会冲突
2. 基础平台的更新不会覆盖业务项目的自定义配置
3. 业务项目的配置可以覆盖或扩展基础平台的配置

## 🎯 解决方案

### 方案 1：分离配置文件（推荐）⭐

**核心思想**：将基础配置和业务配置分离到不同的文件中，通过 CSS 的层叠特性实现覆盖。

#### 目录结构

```
engines/buildx_core/
└── app/assets/tailwind/
    └── base.css                    # 基础平台的基础配置（DaisyUI 插件、基础主题）

app/assets/tailwind/
└── application.css                # 业务项目的完整配置（导入基础配置 + 自定义配置）
```

#### 实现步骤

**1. 基础平台配置**（`engines/buildx_core/app/assets/tailwind/base.css`）

```css
@import "tailwindcss";
@plugin "daisyui" {
  exclude: properties;
}
@plugin "@tailwindcss/typography";

/* 基础平台只定义默认主题，不定义业务特定的主题 */
@plugin "daisyui/theme" {
    name: "light";
    default: true;
    prefersdark: false;
    color-scheme: "light";
    /* ... 基础颜色配置 ... */
}

@plugin "daisyui/theme" {
    name: "dark";
    default: false;
    prefersdark: false;
    color-scheme: "dark";
    /* ... 基础颜色配置 ... */
}

/* 基础平台的通用样式（如 prose 样式） */
.prose {
  /* ... 基础 prose 配置 ... */
}
```

**2. 业务项目配置**（`app/assets/tailwind/application.css`）

```css
/* 导入基础平台的配置 */
@import "../../../engines/buildx_core/app/assets/tailwind/base.css";

/* 业务项目可以覆盖或扩展主题 */
@plugin "daisyui/theme" {
    name: "light";
    default: true;
    prefersdark: false;
    color-scheme: "light";
    /* 覆盖基础平台的主题配置 */
    --color-primary: oklch(55% 0.22 250);
    /* ... 其他自定义配置 ... */
}

/* 业务项目的自定义样式 */
.cardx-custom {
  /* 业务特定的样式 */
}
```

**优点**：
- ✅ 清晰分离：基础配置和业务配置分离
- ✅ 易于维护：基础平台更新不会影响业务配置
- ✅ 灵活扩展：业务项目可以完全自定义

**缺点**：
- ⚠️ 需要手动导入基础配置
- ⚠️ 如果基础平台没有配置文件，需要先创建

### 方案 2：使用 CSS 变量覆盖（当前方案优化）

**核心思想**：基础平台和业务项目都使用相同的配置文件结构，但通过 CSS 变量的优先级实现覆盖。

#### 当前实现

```css
/* app/assets/tailwind/application.css */
@import "tailwindcss";
@plugin "daisyui" { exclude: properties; }
@plugin "@tailwindcss/typography";

/* 主题配置 - 业务项目可以完全自定义 */
@plugin "daisyui/theme" {
    name: "light";
    /* ... 完整主题配置 ... */
}
```

#### 优化建议

**1. 基础平台创建基础配置文件**（可选）

如果基础平台未来需要添加默认主题，可以创建：

```css
/* engines/buildx_core/app/assets/tailwind/base.css */
@import "tailwindcss";
@plugin "daisyui" { exclude: properties; }
@plugin "@tailwindcss/typography";

/* 基础平台只定义默认主题，业务项目可以覆盖 */
@plugin "daisyui/theme" {
    name: "light";
    default: true;
    /* 使用更通用的默认值 */
}
```

**2. 业务项目导入并覆盖**

```css
/* app/assets/tailwind/application.css */
/* 如果基础平台有配置文件，先导入 */
/* @import "../../../engines/buildx_core/app/assets/tailwind/base.css"; */

/* 业务项目的完整配置（会覆盖基础配置） */
@import "tailwindcss";
@plugin "daisyui" { exclude: properties; }
@plugin "@tailwindcss/typography";

@plugin "daisyui/theme" {
    name: "light";
    /* 业务项目的自定义主题 */
}
```

**优点**：
- ✅ 简单直接：业务项目完全控制配置
- ✅ 无需依赖：不依赖基础平台的配置文件

**缺点**：
- ⚠️ 如果基础平台更新主题，业务项目需要手动同步

### 方案 3：使用 Tailwind 配置合并（高级）

**核心思想**：使用 Tailwind CSS 的配置合并功能，将基础配置和业务配置合并。

**注意**：Tailwind CSS 4 使用 CSS 文件配置，不支持传统的 `tailwind.config.js` 合并方式。此方案适用于 Tailwind CSS 3.x。

## 🔧 推荐实施方案

### 方案 4：分离配置文件 + 项目特定的 Theme 名称（最佳实践）⭐⭐

**核心思想**：使用项目特定的 theme 名称（如 `cardx-light`, `cardx-dark`），完全避免与 upstream 的配置冲突。

#### 实现方式

**文件结构**：

```
app/assets/tailwind/
├── application.css    # 基础配置（Tailwind、DaisyUI 插件、Typography 插件）
├── themes.css         # 主题配置（cardx-light, cardx-dark）
└── custom.css         # 自定义样式（prose 样式、代码块样式等）
```

**1. 基础配置**（`app/assets/tailwind/application.css`）

```css
@import "tailwindcss";
@plugin "daisyui" { exclude: properties; }
@plugin "@tailwindcss/typography";

/* 导入项目特定的配置 */
@import "themes.css";
@import "custom.css";
```

**2. 主题配置**（`app/assets/tailwind/themes.css`）

```css
@plugin "daisyui/theme" {
    name: "cardx-light"; /* 项目特定的 theme 名称 */
    default: true;
    /* ... 自定义配置 ... */
}

@plugin "daisyui/theme" {
    name: "cardx-dark"; /* 项目特定的 theme 名称 */
    default: false;
    /* ... 自定义配置 ... */
}
```

**3. 自定义样式**（`app/assets/tailwind/custom.css`）

```css
/* 自定义样式，如 prose 样式、代码块样式等 */
.prose {
  /* ... */
}
```

**2. Theme Controller**（`app/javascript/controllers/theme_controller.js`）

覆盖基础平台的 theme controller，使用项目特定的 theme 名称：

```javascript
// 使用项目特定的 theme 名称
const savedTheme = localStorage.getItem("theme") || "cardx-light"
html.setAttribute("data-theme", savedTheme)
```

**优点**：
- ✅ **完全避免冲突**：
  - 使用项目特定的 theme 名称（`cardx-light`, `cardx-dark`）
  - 即使 upstream 添加了 `light` 和 `dark` theme，也不会冲突
- ✅ **完全隔离配置**：
  - 主题配置在独立的 `themes.css` 文件中
  - 自定义样式在独立的 `custom.css` 文件中
  - 即使 upstream 更新了 `application.css`，也不会影响项目特定的配置
- ✅ **自由自定义**：可以完全自定义主题，无需担心覆盖问题
- ✅ **易于维护**：配置职责清晰，易于理解和维护
- ✅ **未来兼容**：如果 upstream 添加了默认 theme，可以选择使用或忽略

**缺点**：
- ⚠️ 需要覆盖基础平台的 theme controller（但这是必要的，因为 theme 名称不同）

#### 当前实现（CardX 项目）

CardX 项目已采用此方案：
- **文件结构**：
  - `application.css` - 基础配置（Tailwind、DaisyUI 插件、Typography 插件）
  - `themes.css` - 主题配置（完全独立）
  - `custom.css` - 自定义样式（完全独立）
- **Theme 名称**：`cardx-light`（默认）、`cardx-dark`
- **Theme Controller**：`app/javascript/controllers/theme_controller.js`（覆盖基础平台）

**优势**：
- ✅ 即使 upstream 更新了 `application.css`，也不会影响主题配置
- ✅ 配置职责清晰，易于维护
- ✅ 可以独立更新主题和自定义样式

### 当前阶段（基础平台没有 Tailwind 配置）

**保持现状**：
- ✅ 业务项目（`cardx`）完全控制 Tailwind CSS 配置
- ✅ 配置文件位置：`app/assets/tailwind/application.css`
- ✅ 基础平台不包含 Tailwind 配置文件
- ✅ **使用项目特定的 theme 名称**（推荐）

**优势**：
- 简单直接，无需处理配置冲突
- 业务项目可以完全自定义主题和样式
- 即使 upstream 未来添加配置，也不会冲突

### 未来阶段（基础平台需要添加默认配置）

**实施步骤**：

1. **在基础平台创建基础配置文件**
   ```
   engines/buildx_core/app/assets/tailwind/base.css
   ```
   只包含：
   - DaisyUI 插件配置
   - 基础主题（使用通用默认值）
   - 通用样式（如 prose）

2. **业务项目导入基础配置**
   ```css
   /* app/assets/tailwind/application.css */
   @import "../../../engines/buildx_core/app/assets/tailwind/base.css";
   
   /* 业务项目的自定义配置（会覆盖基础配置） */
   @plugin "daisyui/theme" {
       name: "light";
       /* 自定义主题 */
   }
   ```

3. **使用 CSS 层叠特性**
   - 后定义的样式会覆盖先定义的样式
   - 业务项目的配置在基础配置之后，自动覆盖

## 📝 配置管理规范

### 基础平台配置规范

**文件位置**：`engines/buildx_core/app/assets/tailwind/base.css`

**应该包含**：
- ✅ DaisyUI 插件配置
- ✅ Tailwind Typography 插件配置
- ✅ 基础主题配置（使用通用默认值）
- ✅ 通用样式（如 prose 样式）

**不应该包含**：
- ❌ 业务特定的主题配置
- ❌ 业务特定的自定义样式
- ❌ 业务特定的颜色方案

### 业务项目配置规范

**文件位置**：`app/assets/tailwind/application.css`

**应该包含**：
- ✅ 导入基础平台配置（如果存在）
- ✅ 业务项目的自定义主题配置
- ✅ 业务项目的自定义样式
- ✅ 业务特定的扩展样式

**配置优先级**：
1. 业务项目的配置（最高优先级）
2. 基础平台的配置（较低优先级）

## 🔄 合并冲突处理

### 场景 1：基础平台更新主题配置

**问题**：基础平台更新了默认主题，但业务项目有自己的主题配置。

**解决方案**：
- 业务项目的配置会覆盖基础平台的配置
- 如果需要使用基础平台的新配置，业务项目需要手动更新

### 场景 2：基础平台添加新的通用样式

**问题**：基础平台添加了新的通用样式（如 `.prose` 样式），业务项目也有自己的样式。

**解决方案**：
- 如果样式类名不同，不会冲突
- 如果样式类名相同，业务项目的样式会覆盖基础平台的样式
- 建议：基础平台使用命名空间（如 `.buildx-prose`），业务项目使用自己的命名空间（如 `.cardx-prose`）

### 场景 3：基础平台和业务项目都定义了相同的主题名称

**问题**：基础平台定义了 `light` 主题，业务项目也定义了 `light` 主题。

**解决方案**：
- **推荐**：业务项目使用不同的主题名称（如 `cardx-light`），完全避免冲突
- 如果使用相同名称，后定义的主题会覆盖先定义的主题
- 业务项目的主题配置在基础配置之后，会自动覆盖

**CardX 项目采用方案**：使用项目特定的 theme 名称（`cardx-light`, `cardx-dark`），完全避免冲突。

## ✅ 最佳实践

### 1. 使用命名空间

**基础平台**：
```css
/* 使用 .buildx- 前缀 */
.buildx-prose {
  /* ... */
}
```

**业务项目**：
```css
/* 使用项目特定的前缀 */
.cardx-custom {
  /* ... */
}
```

### 2. 文档化配置

在配置文件中添加注释，说明：
- 配置的来源（基础平台或业务项目）
- 配置的用途
- 配置的依赖关系

```css
/* ============================================
   基础平台配置（来自 engines/buildx_core）
   ============================================ */
@import "../../../engines/buildx_core/app/assets/tailwind/base.css";

/* ============================================
   业务项目配置（cardx 项目特定）
   ============================================ */
@plugin "daisyui/theme" {
    name: "light";
    /* cardx 项目的自定义主题 */
}
```

### 3. 使用项目特定的 Theme 名称

**推荐做法**：
- 业务项目使用项目特定的 theme 名称（如 `cardx-light`, `cardx-dark`）
- 完全避免与 upstream 的配置冲突
- 即使 upstream 添加了默认 theme，也不会影响业务项目

**示例**：
```css
/* 业务项目使用项目特定的名称 */
@plugin "daisyui/theme" {
    name: "cardx-light"; /* 而不是 "light" */
    /* ... */
}
```

### 4. 版本控制

- 基础平台的配置变更应该在 CHANGELOG 中记录
- 业务项目的配置变更应该在项目文档中记录
- 重大配置变更应该提供迁移指南

## 🔗 相关文档

- [Tailwind CSS 4 文档](https://tailwindcss.com/docs)
- [DaisyUI 5 文档](https://daisyui.com/docs)
- [Rails 资源管理](https://guides.rubyonrails.org/asset_pipeline.html)

---

**创建时间**：2025-12-03  
**最后更新**：2025-12-03  
**状态**：✅ 方案已确定

